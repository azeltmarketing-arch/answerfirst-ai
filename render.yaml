services:
  - type: web
    name: answerfirst-ai-backend
    env: python
    plan: free
    buildCommand: pip install -r unified/requirements.txt
    startCommand: cd unified && gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORTAL_SECRET
        generateValue: true
      - key: GMAIL_SENDER
        value: azelt.marketing@gmail.com
      - key: GMAIL_APP_PASSWORD
        value: bmwk onnk xszg wbor
      - key: GMAIL_SMTP_SERVER
        value: smtp.gmail.com
      - key: GMAIL_SMTP_PORT
        value: "587"
